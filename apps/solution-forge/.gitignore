# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Power Apps code-app artifacts — env-specific or generated.
# Re-create with `power-apps init` + `pac code add-data-source` (see README).
power.config.json
# Per-env power.config snapshots written by scripts/deploy-env.ps1 (reference only;
# the script's $Registry is the source of truth). Contain env/app/connection ids.
deploy/*.power.config.json
src/generated/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
